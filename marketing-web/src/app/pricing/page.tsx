export default function Page() {
  return (
    <div className="max-w-3xl mx-auto px-6 py-16 prose">
      <h1 className="text-5xl font-extrabold mb-6">Pricing</h1>
      <p className="text-lg whitespace-pre-wrap">Base fare + per-mile + per-minute + surge (if any) − promo + service fee. The exact total shows BEFORE you book. No hidden fees, ever.</p>
    </div>
  );
}
